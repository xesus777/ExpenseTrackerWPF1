<Page x:Class="WpfApp1.Pages.ClientAccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ClientAccountPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Button Content="Назад" Width="60" Click="Back_Click" HorizontalAlignment="Left"/>
            <TextBlock Text="Мой аккаунт" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Мои записи" FontWeight="Bold" Margin="0,0,0,10"/>
                <ListView x:Name="LvAppointments" Grid.Row="1">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Дата" Width="120" DisplayMemberBinding="{Binding AppointmentDateTime}"/>
                            <GridViewColumn Header="Услуга" Width="150" DisplayMemberBinding="{Binding ServiceName}"/>
                            <GridViewColumn Header="Мастер" Width="150" DisplayMemberBinding="{Binding MasterName}"/>
                            <GridViewColumn Header="Цена" Width="80" DisplayMemberBinding="{Binding Price}"/>
                            <GridViewColumn Header="Статус" Width="100" DisplayMemberBinding="{Binding Status}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Мои заказы" FontWeight="Bold" Margin="0,0,0,10"/>
                <ListView x:Name="LvOrders" Grid.Row="1" SelectionChanged="LvOrders_SelectionChanged">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="№ заказа" Width="80" DisplayMemberBinding="{Binding Id}"/>
                            <GridViewColumn Header="Дата заказа" Width="100" DisplayMemberBinding="{Binding OrderDate}"/>
                            <GridViewColumn Header="Дата доставки" Width="100" DisplayMemberBinding="{Binding DeliveryDate}"/>
                            <GridViewColumn Header="Сумма" Width="80" DisplayMemberBinding="{Binding TotalAmount}"/>
                            <GridViewColumn Header="Статус" Width="100" DisplayMemberBinding="{Binding Status}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
