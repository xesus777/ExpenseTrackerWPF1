<Page x:Class="WpfApp1.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">

    <TabControl Margin="10">
        <TabItem Header="Жалобы">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Список жалоб на книги и отзывы" FontWeight="Bold" Margin="0,0,0,10"/>

                <ScrollViewer Grid.Row="1">
                    <ItemsControl x:Name="ComplaintsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10" CornerRadius="5">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold">
                                            <Run Text="Жалоба ID: "/>
                                            <Run Text="{Binding ComplaintID}"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="От пользователя ID: "/>
                                            <Run Text="{Binding UserID}"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="Причина: "/>
                                            <Run Text="{Binding Reason}" FontWeight="Bold"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="Дата: "/>
                                            <Run Text="{Binding CreatedAt}"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="Цель: "/>
                                            <Run Text="{Binding BookID, StringFormat='Книга ID=\{0\}'}"/>
                                            <Run Text="{Binding ReviewID, StringFormat='Отзыв ID=\{0\}'}"/>
                                        </TextBlock>
                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <Button Content="✅ Принять" Click="AcceptComplaint_Click" Tag="{Binding}" Margin="0,0,10,0" Width="100" Background="LightGreen"/>
                                            <Button Content="❌ Отклонить" Click="RejectComplaint_Click" Tag="{Binding}" Width="100" Background="LightCoral"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </TabItem>

        <TabItem Header="Заявки на разморозку">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Список заявок на разморозку аккаунтов и книг" FontWeight="Bold" Margin="0,0,0,10"/>

                <ScrollViewer Grid.Row="1">
                    <ItemsControl x:Name="UnfreezeRequestsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10" CornerRadius="5">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold">
                                            <Run Text="Заявка ID: "/>
                                            <Run Text="{Binding UnfreezeRequestID}"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="От пользователя ID: "/>
                                            <Run Text="{Binding UserID}"/>
                                        </TextBlock>
                                        
                                        <TextBlock TextWrapping="Wrap">
                                            <Run Text="Причина: "/>
                                            <Run Text="{Binding Reason}" FontWeight="Bold"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="Дата: "/>
                                            <Run Text="{Binding RequestDate}"/>
                                        </TextBlock>
                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <Button Content=" Одобрить" Click="AcceptUnfreezeRequest_Click" Tag="{Binding}" Margin="0,0,10,0" Width="100" Background="LightGreen"/>
                                            <Button Content=" Отклонить" Click="RejectUnfreezeRequest_Click" Tag="{Binding}" Width="100" Background="LightCoral"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </TabItem>

        <TabItem Header="Заявки на роль автора">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Список заявок на получение роли Автор" FontWeight="Bold" Margin="0,0,0,10"/>

                <ScrollViewer Grid.Row="1">
                    <ItemsControl x:Name="AuthorRequestsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10" CornerRadius="5">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold">
                                            <Run Text="Заявка ID: "/>
                                            <Run Text="{Binding RequestID}"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="От пользователя ID: "/>
                                            <Run Text="{Binding UserID}"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="Логин пользователя: "/>
                                            <Run Text="{Binding Users.Login}"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="Дата подачи: "/>
                                            <Run Text="{Binding RequestDate}"/>
                                        </TextBlock>
                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <Button Content=" Одобрить" Click="AcceptAuthorRequest_Click" Tag="{Binding}" Margin="0,0,10,0" Width="100" Background="LightGreen"/>
                                            <Button Content=" Отклонить" Click="RejectAuthorRequest_Click" Tag="{Binding}" Width="100" Background="LightCoral"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </TabItem>

        
        <TabItem Header="Пользователи">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox x:Name="SearchBox" Width="200" Margin="0,0,10,0" Tag="Поиск по логину"/>
                    <Button Content="🔍 Найти" Click="SearchUsers_Click" Width="80"/>
                </StackPanel>

                <DataGrid x:Name="UsersGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding UserID}" Width="50"/>
                        <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="120"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                        <DataGridTextColumn Header="Отображаемое имя" Binding="{Binding DisplayName}" Width="120"/>
                        <DataGridTextColumn Header="Роль" Binding="{Binding Roles.RoleName}" Width="100"/>
                        <DataGridCheckBoxColumn Header="Заморожен" Binding="{Binding IsFrozen}" Width="80"/>
                        <DataGridTemplateColumn Header="Действия" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="🔑 Сменить пароль" Click="ChangePassword_Click" Tag="{Binding}" Margin="0,0,5,0" FontSize="10"/>
                                        <Button Content="👑 Сменить роль" Click="ChangeRole_Click" Tag="{Binding}" FontSize="10"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </TabItem>
    </TabControl>
</Page>
