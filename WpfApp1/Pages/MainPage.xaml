<Page x:Class="WpfApp1.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <TextBlock x:Name="UserInfoText" VerticalAlignment="Center" Margin="5"/>
            <Button x:Name="LoginRegisterBtn" Content="Вход" Click="LoginRegisterBtn_Click" Margin="5" Padding="10,5"/>
            <Button x:Name="ProfileBtn" Content="Личный кабинет" Click="ProfileBtn_Click" Margin="5" Padding="10,5" Visibility="Collapsed"/>
            <Button x:Name="LogoutBtn" Content="Выход" Click="LogoutBtn_Click" Margin="5" Padding="10,5" Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="300" Margin="5" TextChanged="SearchBox_TextChanged"/>
            <ComboBox x:Name="SortBox" Width="150" Margin="5" SelectionChanged="SortBox_SelectionChanged">
                <ComboBoxItem Tag="Title">По названию</ComboBoxItem>
                <ComboBoxItem Tag="Rating">По рейтингу</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <ListBox x:Name="MoviesList" Grid.Row="2" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="5" Padding="10" Width="200">
                        <StackPanel>
                            <Image Source="{Binding PosterPath}" Width="Auto" Height="Auto" Stretch="UniformToFill" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" TextWrapping="Wrap"/>
                            <TextBlock Text="Рейтинг: " Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding Rating}"/>
                            <TextBlock Text="Дата: " Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding StartDate, StringFormat={}{0:dd.MM.yyyy}}"/>
                            <TextBlock Text="Возраст: " Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding AgeRatingCode}"/>
                            <Button Content="Подробнее" Click="MovieDetails_Click" Tag="{Binding MovieID}" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
