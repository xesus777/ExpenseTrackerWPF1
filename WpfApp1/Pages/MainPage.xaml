<Page x:Class="WpfApp1.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top panel -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button x:Name="btnLogin" Content="Login" Width="80" Height="30" Margin="5" Click="btnLogin_Click"/>
            <Button x:Name="btnRegister" Content="Register" Width="80" Height="30" Margin="5" Click="btnRegister_Click"/>
            <Button x:Name="btnProfile" Content="Profile" Width="80" Height="30" Margin="5" Click="btnProfile_Click" Visibility="Collapsed"/>
            <Button x:Name="btnLogout" Content="Logout" Width="80" Height="30" Margin="5" Click="btnLogout_Click" Visibility="Collapsed"/>
        </StackPanel>

        <!-- Search and sort panel -->
        <Border Grid.Row="1" Background="#F0F0F0" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtSearch" Grid.Column="0" Height="30" Padding="5" TextChanged="txtSearch_TextChanged"/>

                <TextBlock Grid.Column="1" Text="Sort by:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <ComboBox x:Name="cmbSort" Grid.Column="2" Width="150" Height="30" SelectionChanged="cmbSort_SelectionChanged">
                    <ComboBoxItem Content="Title" IsSelected="True"/>
                    <ComboBoxItem Content="Rating (ascending)"/>
                    <ComboBoxItem Content="Rating (descending)"/>
                </ComboBox>
            </Grid>
        </Border>

        <!-- Movies list -->
        <ListBox x:Name="listMovies" Grid.Row="2" ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="listMovies_SelectionChanged">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="5" Width="200" Background="White">
                        <StackPanel>
                            <Border Background="#E0E0E0" Height="150" Margin="0,0,0,5">
                                <TextBlock Text="[Poster]" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16" TextWrapping="Wrap"/>
                            <TextBlock Text="Rating: " FontWeight="Bold"><Run Text="{Binding Rating}"/></TextBlock>
                            <TextBlock Text="Start date: " FontWeight="Bold"><Run Text="{Binding StartDate, StringFormat={}{0:dd.MM.yyyy}}"/></TextBlock>
                            <TextBlock Text="Age: " FontWeight="Bold">
                                <Run Text="{Binding AgeRatings.AgeRatingCode}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
