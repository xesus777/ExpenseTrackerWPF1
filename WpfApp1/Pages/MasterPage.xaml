<Page x:Class="WpfApp1.Pages.MasterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MasterPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Button Content="Назад" Width="60" Click="Back_Click" HorizontalAlignment="Left"/>
            <TextBlock Text="Панель мастера" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Мои услуги" FontWeight="Bold"/>
                <Button Grid.Column="1" Content="Изменить услуги" Width="100" Click="EditServices_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Мои записи" FontWeight="Bold" Margin="0,0,0,10"/>
                <ListView x:Name="LvAppointments" Grid.Row="1" SelectionChanged="LvAppointments_SelectionChanged">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Дата" Width="120" DisplayMemberBinding="{Binding AppointmentDateTime}"/>
                            <GridViewColumn Header="Услуга" Width="150" DisplayMemberBinding="{Binding ServiceName}"/>
                            <GridViewColumn Header="Клиент" Width="150" DisplayMemberBinding="{Binding ClientName}"/>
                            <GridViewColumn Header="Телефон" Width="100" DisplayMemberBinding="{Binding ClientPhone}"/>
                            <GridViewColumn Header="Статус" Width="100" DisplayMemberBinding="{Binding Status}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
