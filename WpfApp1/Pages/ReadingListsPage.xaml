<Page x:Class="WpfApp1.Pages.ReadingListsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ReadingListsPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="SectionSelector" Height="Auto" Margin="0,0,0,10" SelectionChanged="SectionSelector_SelectionChanged">
            <ListBoxItem Content="В планах"/>
            <ListBoxItem Content="Читаю"/>
            <ListBoxItem Content="Прочитано"/>
            <ListBoxItem Content="Заброшено"/>
        </ListBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="200" Margin="5" Tag="Поиск"/>
            <ComboBox x:Name="SortBox" Width="120" Margin="5" SelectedIndex="0">
                <ComboBoxItem Content="По названию"/>
                <ComboBoxItem Content="По оценке"/>
            </ComboBox>
            <ComboBox x:Name="GenreFilter" Width="150" Margin="5" DisplayMemberPath="GenreName"/>
            <Button Content="Применить" Click="ApplyFilter_Click" Margin="5"/>
        </StackPanel>

        <ItemsControl x:Name="BooksGrid" Grid.Row="2">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="5" Width="200">
                        <StackPanel>
                            <TextBlock Text="{Binding Books.Title}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Books.Users.DisplayName}" FontSize="11"/>
                            <ComboBox x:Name="MoveToList" SelectedItem="{Binding Section}" Width="120" Margin="0,5" SelectionChanged="MoveToList_SelectionChanged" Tag="{Binding}">
                                <ComboBoxItem Content="В планах"/>
                                <ComboBoxItem Content="Читаю"/>
                                <ComboBoxItem Content="Прочитано"/>
                                <ComboBoxItem Content="Заброшено"/>
                            </ComboBox>
                            <Button Content="Открыть" Click="OpenBook_Click" Tag="{Binding Books}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
