<Page x:Class="WpfApp1.Pages.ReadingListsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ReadingListsPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="SectionSelector" Height="Auto" Margin="0,0,0,10" SelectionChanged="SectionSelector_SelectionChanged">
            <ListBoxItem Content="В планах"/>
            <ListBoxItem Content="Читаю"/>
            <ListBoxItem Content="Прочитано"/>
            <ListBoxItem Content="Заброшено"/>
        </ListBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="200" Margin="5"/>
            <ComboBox x:Name="SortBox" Width="120" Margin="5" SelectedIndex="0">
                <ComboBoxItem Content="По названию"/>
                <ComboBoxItem Content="По оценке"/>
            </ComboBox>
            <ComboBox x:Name="GenreFilter" Width="150" Margin="5" DisplayMemberPath="GenreName"/>
            <Button Content="Применить" Click="ApplyFilter_Click" Margin="5"/>
        </StackPanel>

        <ItemsControl x:Name="BooksGrid" Grid.Row="2">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="5" Width="200">
                        <StackPanel>
                            <Image Source="{Binding ReadingList.Books.CoverImagePath}" 
                                   Width="150" 
                                   Height="200" 
                                   Stretch="UniformToFill"
                                   Margin="0,0,0,5">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Setter Property="Source" Value="/Images/no_cover.png"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ReadingList.Books.CoverImagePath}" Value="{x:Null}">
                                                <Setter Property="Source" Value="/Images/no_cover.png"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ReadingList.Books.CoverImagePath}" Value="">
                                                <Setter Property="Source" Value="/Images/no_cover.png"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>

                            <TextBlock Text="{Binding ReadingList.Books.Title}" FontWeight="Bold" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding ReadingList.Books.Users.DisplayName}" FontSize="11" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding RatingText}" FontSize="11" Foreground="Gold" Margin="0,5,0,0"/>

                            <ComboBox x:Name="MoveToList" Width="120" Margin="0,5" SelectionChanged="MoveToList_SelectionChanged" Tag="{Binding}">
                                <ComboBoxItem Content="В планах"/>
                                <ComboBoxItem Content="Читаю"/>
                                <ComboBoxItem Content="Прочитано"/>
                                <ComboBoxItem Content="Заброшено"/>
                            </ComboBox>
                            <Button Content="Открыть" Click="OpenBook_Click" Tag="{Binding}" Margin="0,5"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
