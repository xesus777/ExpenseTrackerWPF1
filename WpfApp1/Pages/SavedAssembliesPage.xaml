<Page x:Class="WpfApp1.Pages.SavedAssembliesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SavedAssembliesPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Назад к сборке" Padding="5" Click="Back_Click"/>
        </StackPanel>

        <ListView x:Name="AssembliesListView" Grid.Row="1" SelectionChanged="AssembliesListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" DisplayMemberBinding="{Binding name}" Width="200"/>
                    <GridViewColumn Header="Автор" DisplayMemberBinding="{Binding author}" Width="150"/>
                    <GridViewColumn Header="Кол-во деталей" DisplayMemberBinding="{Binding partassembly.Count}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
