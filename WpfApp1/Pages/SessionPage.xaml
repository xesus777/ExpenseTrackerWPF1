<Page x:Class="WpfApp1.Pages.SessionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SessionPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock x:Name="txtSessionInfo" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="txtPrice" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="seatsContainer">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" MaxWidth="550"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button x:Name="btnSeat" 
                                Width="50" Height="50" Margin="2" 
                                Click="Seat_Click"
                                Tag="{Binding SeatID}">
                            <StackPanel>
                                <TextBlock Text="{Binding SeatRow}" HorizontalAlignment="Center" FontSize="10"/>
                                <TextBlock Text="{Binding SeatNumber}" HorizontalAlignment="Center" FontWeight="Bold" FontSize="14"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="btnClear" Content="Clear selection" Width="120" Height="30" Margin="5" Click="btnClear_Click"/>
            <Button x:Name="btnBook" Content="Book ticket" Width="120" Height="30" Margin="5" Click="btnBook_Click" Background="LightGreen"/>
            <Button x:Name="btnBack" Content="Back to movie" Width="120" Height="30" Margin="5" Click="btnBack_Click"/>
        </StackPanel>
    </Grid>
</Page>
