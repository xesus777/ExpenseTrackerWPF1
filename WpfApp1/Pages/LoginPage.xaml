<Page x:Class="WpfApp1.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Назад" Width="60" Margin="0,0,0,20" Click="Back_Click" HorizontalAlignment="Left"/>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="20" 
            HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <StackPanel>
                <TextBlock Text="Авторизация" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <TextBlock Text="Логин" Margin="0,5"/>
                <TextBox x:Name="TxtLogin" Height="25" Margin="0,5"/>

                <TextBlock Text="Пароль" Margin="0,10,0,5"/>
                <PasswordBox x:Name="TxtPassword" Height="25" Margin="0,5"/>

                <Button Content="Войти" Height="30" Margin="0,15" Click="BtnLogin_Click"/>

                <TextBlock x:Name="TxtError" Foreground="Red" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
