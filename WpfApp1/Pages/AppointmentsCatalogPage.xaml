<Page x:Class="WpfApp1.Pages.AppointmentsCatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AppointmentsCatalogPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Назад" Width="60" Click="Back_Click"/>
            <TextBlock Grid.Column="1" Text="Запись на услуги" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Мастер"/>
                    <ComboBox x:Name="CmbMasters" DisplayMemberPath="FullName" SelectedValuePath="Id" 
                          SelectionChanged="CmbMasters_SelectionChanged" Height="25" Margin="0,5"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Услуга"/>
                    <ComboBox x:Name="CmbServices" DisplayMemberPath="Name" SelectedValuePath="Id" 
                          SelectionChanged="CmbServices_SelectionChanged" Height="25" Margin="0,5" IsEnabled="False"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Дата"/>
                    <DatePicker x:Name="DatePickerFilter" SelectedDateChanged="DatePickerFilter_SelectedDateChanged" Height="25" Margin="0,5"/>
                </StackPanel>

                <Button x:Name="BtnSearch" Grid.Column="3" Content="Поиск" Width="80" Height="25" 
                    Margin="10,20,0,0" Click="BtnSearch_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Доступное время для записи" FontWeight="Bold" Margin="0,0,0,10"/>

                <ListView x:Name="LvAvailableSlots" Grid.Row="1" SelectionChanged="LvAvailableSlots_SelectionChanged">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Дата и время" Width="150" DisplayMemberBinding="{Binding TimeDisplay}"/>
                            <GridViewColumn Header="Услуга" Width="200" DisplayMemberBinding="{Binding ServiceName}"/>
                            <GridViewColumn Header="Мастер" Width="200" DisplayMemberBinding="{Binding MasterName}"/>
                            <GridViewColumn Header="Цена" Width="100" DisplayMemberBinding="{Binding Price}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
