<Window x:Class="WpfApp1.SelectListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Выберите список" Height="450" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="BookTitleText" 
                   FontWeight="Bold" 
                   FontSize="14" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Выберите раздел:" Margin="0,0,0,10"/>
            <ListBox x:Name="SectionsList" 
                     SelectionMode="Single"
                     Height="120"
                     SelectionChanged="SectionsList_SelectionChanged">
                <ListBoxItem Content="В планах"/>
                <ListBoxItem Content="Читаю"/>
                <ListBoxItem Content="Прочитано"/>
                <ListBoxItem Content="Заброшено"/>
            </ListBox>
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="0,10,0,0">
                <Button x:Name="OkBtn" 
                        Content="OK" 
                        Width="80" 
                        Margin="5"
                        IsEnabled="False"
                        Click="OkBtn_Click"/>
                <Button x:Name="CancelBtn" 
                        Content="Отмена" 
                        Width="80" 
                        Margin="5"
                        Click="CancelBtn_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
